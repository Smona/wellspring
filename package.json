{
  "scripts": {
    "start": "http-server -o",
    "triple-maps": "node tools/create-wrapped-tilemap.js tilemaps/level1.json",
    "build": "rm -rf build/ && mkdir build && npm run triple-maps && zip -r build/wellspring public/"
  },
  "dependencies": {
    "http-server": "^0.10.0"
  }
}
